import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  income: {
    borderBottom: '10px solid rgba(255,0,0,0.7)',
  },
  expenses: {
    borderBottom: '10px solid rgb(0,100,0,0.8)',
  },
}));
